DECLARE
	a POSITIVE(3):=444;
	b NUMBER(3):=222;
	c CHAR:='c';
BEGIN
	WHILE a>b LOOP
		BEGIN
			a:=2+2+4;
			b:=8;
			DBMS_OUTPUT.PUT(a);
			DBMS_OUTPUT.NEW_LINE;
		END;
	END LOOP;
	--a:=VARCHAR2(2)(a);
	IF BEGIN a<b THEN
		DBMS_OUTPUT.PUT(a);
	END IF;
END;\
